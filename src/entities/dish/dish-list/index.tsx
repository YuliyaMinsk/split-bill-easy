import { Container } from '@mui/material';
import { Dish } from '../dish';

const DishList = (): JSX.Element => {
  return (
    <Container>
      <Dish />
    </Container>
  );
};

export { DishList };
