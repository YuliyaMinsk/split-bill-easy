import styled from '@emotion/styled';
import { Box } from '@mui/material';

const StyledBox = styled(Box)`
  margin-top: 1rem;
  margin-left: 1rem;
  margin-right: 1rem;
  margin-bottom: 2rem;
  text-align: center;
`;

export { StyledBox };
